package com.karabow.zing;

public interface KeyboardListener
{
	/*
	 * setKeyboardState and getKeyboardState are used to set and check the states of the keyboard
	 */

	public void isBackKeyPressed(boolean yes);

	
}
