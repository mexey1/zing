<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="AnimationPopup" parent="@android:style/Widget.PopupWindow">
        <item name="android:windowAnimationStyle">@style/ZingNotification</item>
    </style>
    <style name="ZingNotification" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/notification_enter</item>
        <item name="android:windowExitAnimation">@anim/notification_leave</item>
    </style>
    
    <style  name="textview_style" parent="@android:style/TextAppearance.Medium">
        <item name="android:gravity">right</item>
        
    </style>

</resources>
